@page
@model RazorPagesDevice.Pages.Devices.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Device[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Device[0].DeviceTypes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Device[0].DeviceTypeProperties)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Device[0].DevicesTypeValues)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Device) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DeviceTypes)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DeviceTypeProperties)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DevicesTypeValues)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
